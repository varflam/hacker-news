import React from 'react';

import './button.sass';

const Button = () => {
    return (
        <button type="button" className='button'>
            Update
        </button>
    );
};

export default Button;